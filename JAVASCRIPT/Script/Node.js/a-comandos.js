/*
    Node.js; es un entorno de ejecución JavaScript de código abierto y multiplataforma que se utiliza para desarrollar aplicaciones escalables del lado del servidor y de red.

    Podemos utilizat JavaScript para darle comportamiento y funcionalidad a nuestra pagina web. 
    -> Animaciones, Menus desplegables, etc.

    Pero node, nos permite utilizar JS para interactuar con el hardware de la computadora.
    -> Aplicaciones de escritorio (Atom), lo cual nos permite escribir aplicaciones completas.
*/

/*  
    Utilizar Node con hyper.

    node "name.extension" -> Usamos node para ejecutar el archivo.

    REPL -> Red Evaluation Print Loop.
    Permite ejecutar codigo en trozos pequeños como en la consola de google.

    con el comando "node" podemos activar REPL dentro de nuestro Hyper.

    Para salir del REPL, simplemente escribimos .exit ó ctrl + c dos veces o ctrl + d.
*/

/*
    NPM -> Node Package Manager
    Sistema de gestion de paquetes de Node.js

    Los paquetes en NPM, son fragmentos de codigo reutilizables que alguien mas escribio, usando NPM, podemos incorporar esos paquetes en proyectos propios.

    npm init -> inicializamos npm en nuestro proyecto. una vez desarrollado la inicializacion, se habra creado un archivo json "package.json"

    package.json -> cuando instalemos nuevos paquetes externos, apareceran dentro de este archivo.
*/

