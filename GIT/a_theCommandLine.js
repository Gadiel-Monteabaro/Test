/*
    Command Line

    Kernel -> Se refiere al programa real que interactua con el Hardware.
    Shell -> Se refiere a la interfaz de usuario.

    Bash -> Bourne Again Shell. Interprete de comandoS para el sistema operativo UNIX.
*/

// Lineas de Comandos.

/* acceso a los directorios. 
    ls -> Enumera todos lo elementos dentro de un directorio en particular.
    ls -a -> Muestra todos los archivos incluyendo los archivos ocultos.
    cd -> Directorio de cambio.
    cd .. -> Retrocedemos de directorio.
    cd "ruta" -> accdemos al directorio de una ruta establecida. 
*/

/* creacion de directorios.
    mkdir "name" -> crea una nueva carpeta.
    touch "name.extension" -> crea un nuevo archivo.
    start "name.extension" -> abre el archivo.
    rm "name.extension" -> Borrar archivo.
    rm * -> borra todos los archivos del directorio.
    rm -r "directoriName" -> borra el directorio.
    pwd -> imprimir directorio de trabajo (traducido)
*/

